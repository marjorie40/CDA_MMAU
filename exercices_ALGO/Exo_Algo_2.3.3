// demander 2 valeurs au User les * puis indiquer si resultat + ou - 

ALGO_2_3

Variable A, B, resultat : entiers

Début 

Ecrire (Veuillez entrer un nombre A)
Lire (A)
Ecrire (Veuillez entrer un nombre B)
Lire (B) 

resultat = A*B 

    Si (resultat = 0) Alors
        ecrire ("le resultat est nul")

    sinon
        Si resultat > 0 Alors
            ecrire ("le résultat est positif)

        sinon
            Si resultat < 0 Alors 
            ecrire ("le resultat est négatif")

        FinSi
    FinSi

Fin