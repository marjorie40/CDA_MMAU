
N : entier // Déclaration de la variable N de type entier
N := lireEntier () // Lecture d'un entier et affectation de la variable N


// BLOC DECLARATION
    variable saisieUtilisateur : entier // Déclaration de la variable N de type entier 
    variable resultat : entier
    variable compteur : entier  

    début
        resultat = 0
        compteur = 1

        saisieUtilisateur = lireEntier() // Identifiaction N saisi par l'utilisateur
        
        TANTQUE compteur <= saisieUtilisateur FAIRE
            resultat = resultat + compteur
            compteur = compteur + 1
        finTantque
        ecrire ("Le résultat est :", resultat); // Résultat de l'opération
    fin

  

variable N : entier
Tantque <N<6> faire // on arrete tant que N>6

    début
        lireEntier(N) // Identifiaction N saisi par l'utilisateur
        resultat = N+(N+1) // Opération à effectuer
    fin
finTantque
ecrire ("Le résultat avec tantque est :", resultat); // Résultat de l'opération
